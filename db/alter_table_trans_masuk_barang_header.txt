1. Perubahan field `id` pada tabel `trans_masuk_barang_header` menjadi AUTO_INCREMENT
2. Perubahan datatype pada field `tgl_masuk` di tabel `trans_masuk_barang_header` dari DATETIME menjadi DATE
3. Penambahan field `gudang` VARCHAR(255) di tabel `trans_masuk_barang_header`